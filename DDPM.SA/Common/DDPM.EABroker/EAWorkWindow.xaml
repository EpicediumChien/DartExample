<Window x:Class="DDPM.EABroker.EAWorkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DDPM.EABroker"
        mc:Ignorable="d"
        Title="EAWorkWindow" Height="450" Width="800"
        AllowsTransparency="True" Topmost="True"
        WindowStyle="None" ResizeMode="NoResize" Background="Transparent" ShowInTaskbar="False" 
        Loaded="Window_Loaded"
        >
    <Window.Resources>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetName="fadeOutCtrl" Storyboard.TargetProperty="Opacity"
                             BeginTime="0:0:0.5" Duration="0:0:3"
                             From="1.0" To="0" FillBehavior="HoldEnd" />
        </Storyboard>

    </Window.Resources>  
    <Grid>
        <!-- Robert_Lin, 2024-10-20 Set BorderThickness="1" can help engineer to check if the EAWworkWindow position is correct -->
        <Border BorderBrush="Red" BorderThickness="0">
            <Grid Name="splitGrid">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsWorkWindowVisible}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                                <Setter Property="Window.Topmost" Value="True" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <ContentControl Name="splitCtrl" />
                <Canvas Name="canvasWorker"/>
            </Grid>
        </Border>

        <Grid Name="fadeOutGrid" Visibility="Hidden">
            <!-- 
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Hidden" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsFading, RelativeSource={RelativeSource AncestorType=Window}}"
                                     Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            -->
            <ContentControl Name="fadeOutCtrl" />
            <Canvas Name="canvasFader"/>
        </Grid>
    </Grid>
</Window>
